# Universal Vocabulary

Editable shared multilingual lexicon for Nova.

Primary file: `src/vocabulary.json`.

Add universal human-language variants here. Add business-specific product/service jargon to the tenant's `offerings/items.json` aliases or Knowledge Layer output instead.

The central matcher is deliberately data-driven so vocabulary can grow without changing capability JavaScript.
