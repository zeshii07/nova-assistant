const test = require("node:test"); const assert = require("node:assert/strict");
const { buildContainer } = require("../apps/api/src/container");
test("execution engine routes through discovered assistant capability", async () => { const container = await buildContainer(); const result = await container.executionEngine.process({ tenantId:"default", customerId:"user-1", channel:"http", text:"hello" }); assert.equal(result.capabilityId, "assistant"); assert.match(result.reply, /hello|assalam/i); assert.equal(result.state.context.lastCapability, "assistant"); await container.registry.shutdownAll(); });
test("assistant event is emitted", async () => { const container = await buildContainer(); let emitted = false; container.eventBus.subscribe("assistant.responded.v1", () => { emitted = true; }); await container.executionEngine.process({ tenantId:"default", customerId:"user-2", channel:"http", text:"thanks" }); assert.equal(emitted, true); await container.registry.shutdownAll(); });
