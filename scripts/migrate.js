const fs=require('fs');const path=require('path');const {loadConfig}=require('../packages/config/src/config');const {PostgresClient}=require('../packages/storage/src/postgresClient');
(async()=>{const config=loadConfig();if(!config.databaseUrl)throw new Error('DATABASE_URL is required');const db=new PostgresClient({connectionString:config.databaseUrl});await db.connect();const dir=path.resolve(__dirname,'../database/migrations');for(const name of fs.readdirSync(dir).filter(x=>x.endsWith('.sql')).sort()){console.log(`Applying ${name}`);await db.query(fs.readFileSync(path.join(dir,name),'utf8'));}await db.close();console.log('Migrations complete.');})().catch(e=>{console.error(e);process.exitCode=1;});
